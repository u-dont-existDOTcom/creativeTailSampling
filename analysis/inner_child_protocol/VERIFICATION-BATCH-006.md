# Verification report — Inner-child map hardening Batch 006

**Date:** 2026-08-17  
**Working branch:** `agent/inner-child-real-query-hardening-006`  
**Pre-verification-record head:** `13a03d2965b4763b4734c9d1cd58f48871f152ad`  
**Result:** PASS within the stated boundary

## Live GitHub readback

- Canonical overview exists and states **one inner parent / integrated adult with three qualities**.
- Overview routes actor/beneficiary, immediate danger, and primary problem class before internal-state formulation.
- Focused map directory contains maps `00` through `14`.
- Batch 006 corpus directory contains case files `RQ6-01.json` through `RQ6-16.json`.
- Live manifest parses as a JSON object and declares:
  - schema `real-unprimed-therapy-query-benchmark-v2`;
  - `case_count: 16`;
  - 16 case paths;
  - original-post-only/unprimed corpus policy;
  - pre-Batch semantic checkpoint `db591713a3feb0a1576943408ae356685c0034ec`.
- Live readback of `RQ6-16.json` confirms that model input (`query`) is separated from expected route, defect, correction, and regression assertions.
- README, dedicated state, fresh-conversation handoff, run record, InnerSignalGraph addendum, gap/evidence supplements, and benchmark plan all exist on the branch.
- The branch is 72 commits ahead and 0 behind `main`; its merge base is the current `main` head.

## Deterministic package checks before upload

- 16 unique real-query case IDs.
- 16 distinct source URLs.
- Zero target-framework priming terms added to benchmark query strings.
- Seven new/replaced Mermaid blocks structurally parsed for:
  - balanced code fences and quotes;
  - unique node definitions;
  - no undefined edge references.
- Overview links 15 focused maps.
- All four residual Batch 006 defects are represented in active map text:
  - minor/guardian duty versus accommodation;
  - numbness versus decision competence;
  - collateral-information permission;
  - proportional decision slowing.
- Article prose is unchanged.

## Diff review

The `main...agent/inner-child-real-query-hardening-006` comparison contains the complete prior inner-child protocol lane plus Batch 006. It does not include the divergent federation-ecology lane. New Batch 006 artifacts are confined to the inner-child protocol workspace, its docs/run records, and README indexing.

## Boundary

This verifies repository structure, provenance, benchmark completeness, and internal map consistency. It does **not** establish clinical efficacy, deployment safety, diagnostic accuracy, or the behavior of the not-yet-merged InnerSignalGraph implementation.

A full Mermaid rendering binary was not available in the producing environment. GitHub rendering and any executable graph compilation remain part of the downstream repository/PR verification.
